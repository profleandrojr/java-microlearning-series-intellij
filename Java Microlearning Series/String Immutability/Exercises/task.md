## Exercises

Answer the questions by assigning the correct option text to each `answer` variable.

Use **exactly one of the provided options** for each question.

---

### Question 1
What happens when a String in Java loses all references?

**Options:**

A) "The String object is immediately destroyed by the JVM."

B) "The String object becomes eligible for garbage collection.

C) "The String object is modified in place."

D) "The String object is moved to the String Pool."

---

### Question 2
Why does the following code not change the value of `greeting`?

```java
greeting.concat("!!!");
``` 

**Options:**

A) "concat() works only with StringBuilder."

B) "Strings are immutable and concat() returns a new String."

C) "Printing is required for concat() to work."

D) "Local variables cannot be reassigned."

---

### Question 3

What actually changes when we write:
```java
greeting = greeting.substring(0, n);
```

**Options:**

A) "The original String object is modified."

B) "The JVM replaces the String in the String Pool."

C) "The variable now references a new String object."

D) "The memory address of the original object changes."

---

## Rules

Use only the provided options.

Do NOT change the print statements.

Your answers must match the chosen option text exactly.